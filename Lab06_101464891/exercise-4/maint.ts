import { Customer } from './customer';

// Create an instance of the Customer class with age
let customer = new Customer("John", "Smith", 30);

// Call the greeter method
customer.greeter();

// Call the GetAge method to log the customer's age
customer.GetAge();
